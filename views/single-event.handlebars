
<div class="container">
<div class="row">
<div class="col-md-8">
<div class="card my-5">
  <h5 class="card-header">{{event.name}}</h5>

  <div class="card-body">
  <p>      {{event.description}}
</p>
 </div>
 <ul class="list-group list-group-flush">
    <li class="list-group-item">Date: {{event.event_date}}</li>
    <li class="list-group-item">Category:{{event.category}}</li>
    <li class="list-group-item">Covid Protocol: {{event.covid_items}}</li>
  </ul>

 
  <div class="card-footer">
                  <div class="d-flex justify-content-between align-items-center">
                  <div class="btn">
                    <button type="button" id="join-btn" class="btn btn-sm btn-outline-secondary">Join</button>
                    <button type="button" id="comment-btn" class="btn btn-sm btn-outline-secondary">Comment</button>
                  </div>
    <figure class="text-end pt-3">

      <figcaption class="blockquote-footer ms-5 pe-5">
        Created by: {{event.User.username}}

      </figcaption>
    </figure>
                </div>
  </div>
</div>
</div>
<div class="col-md-3">
<div class="card px-3 my-5">
   

    <ul class="list-group list-group-flush">
    {{#each events as |event| }}
    <li class="list-group-item"><a href="/event/{{id}}">{{event.name}}</a> | {{event.event_date}}</li>
              {{/each}}

    </ul>
side bar
</div>
</div>
</div>
</div>