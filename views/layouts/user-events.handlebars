
    <div class="album py-5">
      <div class="container">

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        {{#each events}}
          <div class="col">
            <div class="card shadow-sm">
              <img src="https://via.placeholder.com/225" height="225">
              <div class="card-body">
                <h5 class="card-title"><a href="/event/{{id}}">{{event.name}}</a> <span class="float-end text-muted">{{event.event_date}}</span></h5>
                <p class="card-text">{{event.description}}</p>
                <div class="d-flex justify-content-between align-items-center">
                  <div class="btn">
                          <a class="btn btn-danger" href="/dashboard/edit/{{id}}">Edit Event</a>
    </div>
                  </div>
                  <small class="text-muted">{{event.category}}</small>

                </div>
              </div>
            </div>
          </div>              
          {{/each}}
          </div>
            </div>
          </div>